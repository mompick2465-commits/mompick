# 맘픽 관리자 페이지 환경 변수 설정 예제
# 이 파일을 복사하여 .env.local 파일을 만들고 실제 값으로 채워주세요.
# cp .env.example .env.local

# ============================================
# Supabase 설정
# ============================================
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ============================================
# Supabase 서비스 키 (관리자 권한)
# ============================================
# ⚠️ 주의: 이 키는 서버 사이드에서만 사용되며, 절대 클라이언트에 노출되면 안 됩니다.
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ============================================
# Next.js 설정 (선택적)
# ============================================
NEXTAUTH_URL=http://localhost:3001
NEXTAUTH_SECRET=your_nextauth_secret_here

# ============================================
# 관리자 인증 설정
# ============================================
# 관리자 인증 토큰 (프로덕션 환경 필수)
# 강력한 랜덤 문자열 생성: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_SECRET_TOKEN=your_admin_secret_token_here

# 개발 환경에서만 인증 우회 (프로덕션에서는 설정하지 않음)
# 개발 중: ADMIN_BYPASS_AUTH=true
# 프로덕션: 이 변수를 설정하지 않음
ADMIN_BYPASS_AUTH=false

# ============================================
# 관리자 계정 설정
# ============================================
# 관리자 로그인 이메일
ADMIN_EMAIL=admin@mompick.com

# 관리자 로그인 비밀번호
ADMIN_PASSWORD=your_secure_password_here

# 개발 환경에서만 인증 우회 (프로덕션에서는 설정하지 않음)
# 개발 중: ADMIN_BYPASS_AUTH=true
# 프로덕션: 이 변수를 설정하지 않음
ADMIN_BYPASS_AUTH=false
